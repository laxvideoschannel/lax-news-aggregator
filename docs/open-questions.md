# Open Questions

## Product
- Should college be represented as `school` only, or `school + team/program`?
- Should men’s and women’s college be separated in nav from day one?
- Should training live in top nav at launch, or wait until enough provider content exists?

## Content sourcing
- Which college teams/schools are first targets?
- Will college video be official YouTube embeds only at first?
- Which training providers are credible enough to include first?

## Merch
- Keep only official links for now?
- When should original merch be introduced?

## UX
- Should `/team` remain the route name once college is added, or should there be `/teams` and `/schools`?
- Should `/videos` become the primary entry point of the product?

## Data model
- How should `league`, `school`, and `team` relate?
- Does an `event` need to support both games and training from v1 of the model?
